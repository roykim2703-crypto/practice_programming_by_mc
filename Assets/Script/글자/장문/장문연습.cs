using UnityEngine;

public class 장문연습 : MonoBehaviour
{
    [System.Serializable]
    public class MyData
    {
        public string text;
    }

    public MyData myData;


    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
